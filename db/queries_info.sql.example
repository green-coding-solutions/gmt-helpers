-- Separate queries in this file via newline + ###### + newline

-- Check who is currently on the watchlist
SELECT * FROM watchlist WHERE schedule_mode = 'weekly' AND created_at > NOW() - INTERVAL '25 HOURS'

######